REDIS_URL=redis://localhost:6379/0
PORT=8080
LAN_HOST=192.168.111.21
PRICE_PER_KM=200
MAX_UPLOAD_MB=10

# Server-side only. Enable Places API (New), Geocoding API and Routes API,
# then restrict this key to those APIs and, in production, the API server IP.
GOOGLE_MAPS_API_KEY=your_server_maps_key
